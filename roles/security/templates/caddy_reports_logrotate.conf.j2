{{ caddy_proxy.csp_reports_log_path }}/*.log {
    rotate 10
    weekly
    missingok
    notifempty
    compress
    delaycompress
    postrotate
        systemctl reload caddy >/dev/null 2>&1 || true
    endscript
}